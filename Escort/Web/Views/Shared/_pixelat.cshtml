

    <style>
        #image-container {
            position: relative; height:580px; width:100%; float:left
        }

            #image-container canvas {
                position: absolute;
                top: 0;
                left: -12px;
                border: 2px solid #000;
            }

            #image-container img {
                display: none;
            }
        /* Modal styles */

    .modalpixlate {
            display: none;
            position: fixed;
            z-index: 1;
            padding-top: 60px;
            left: 0;
            top: 0;
            width: 100%;
            height: 100%;
            background-color: rgb(0, 0, 0);
            background-color: rgba(0, 0, 0, 0.4);
        }

        .modal-content {
        background-color: #e6d8ca;
            margin: 5% auto;
            padding: 20px;
        border: 1px solid #e6d8ca;
            max-width:500px;
        border-radius: 10px;
        }

        .close {
            color: #aaa;
            float: right;
            font-size: 28px;
            font-weight: bold;
        }

            .close:hover,
            .close:focus {
                color: black;
                text-decoration: none;
                cursor: pointer;
            }
    </style>


<!-- Modal -->

   
 

    <!-- The Modal -->
       <div id="pixlateModal" class="modal" >
    <div class="modal-dialog modal-lg" role="document">
        <div class="modal-content">
            <div class="modal-body" style="padding-bottom:0px">
                <div class="row">
                    <div class="col-md-12 text-center">
                        <div id="image-container">
                            <img id="original-image" alt="Original Image">
                            <canvas id="pixelated-canvas"></canvas>
                        </div>
                    </div>
                    <div class="col-md-12 text-end">
                        <button class="btn btn-outline-primary" data-bs-dismiss="modal" aria-label="Close" id="close-button">Close</button>
                        <button class="btn btn-primary" id="save-button">Save</button>
                    </div>
                </div>
            </div>
            
        </div>

    </div>
        <!-- Modal content -->
        
    </div>

  
